# ## Secrets and Environment Variables ##
# Prevents uploading your MongoDB URI and other secret keys.
.env
.env.local

# ## Dependencies ##
# This single line ignores BOTH /client/node_modules and /server/node_modules.
node_modules

# ## Production Build ##
# Ignores the build output folder from your client-side app (React/Vite).
build
dist

# ## Logs ##
# Ignores log files that might be generated.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ## System Files ##
# Ignores system-specific files.
.DS_Store